from langchain_core.messages import HumanMessage


def build_context(messages: list, max_history: int = 5) -> str:
    lines = []
    for msg in messages[-max_history:]:
        role = "Клиент" if isinstance(msg, HumanMessage) else "Агент"
        lines.append(f"{role}: {msg.content[:500]}")
    return "\n".join(lines)


CLASSIFICATION_PROMPT = """Ты — классификатор запросов для туристического агентства.

Классифицируй сообщение клиента в одну из категорий:
- tour_search: клиент ищет тур, спрашивает о направлениях, ценах, датах
- faq: клиент задаёт общий вопрос (визы, страховка, погода, документы, правила въезда)
- complaint: клиент жалуется, недоволен, просит решить проблему
- talk_to_manager: клиент прямо просит поговорить с живым человеком / менеджером / оператором
- booking: клиент хочет записаться / забронировать / оплатить
- greeting: клиент просто здоровается (привет, здравствуйте, добрый день)
- unknown: не удалось определить

Также проверь, не содержит ли сообщение триггеров эскалации:
- Слова: "срочно", "жалоба", "недоволен", "проблема"
- Запрос индивидуального подбора
- Слишком сложный/нестандартный запрос

Ответь строго в формате JSON:
{{
  "request_type": "tour_search|faq|complaint|talk_to_manager|booking|greeting|unknown",
  "needs_escalation": false,
  "escalation_reason": null
}}

Контекст диалога (последние сообщения):
{context}

Сообщение клиента:
{message}
"""

EXTRACT_PROMPT = """Извлеки из сообщения клиента параметры тура. Если параметр не указан — оставь null.

Параметры:
- destination: страна или город (например: "Турция", "Анталья")
- dates: даты или месяц/сезон (например: "август", "15-22 июня")
- budget: бюджет в долларах (например: "2000", "до 1500")
- travelers: количество человек (только число)
- tour_type: тип отдыха (например: "пляж", "экскурсии", "горы", "круиз")

Ответь строго JSON:
{{
  "destination": null,
  "dates": null,
  "budget": null,
  "travelers": null,
  "tour_type": null,
  "missing_params": ["список отсутствующих параметров — destination, dates, budget, travelers, tour_type"]
}}

Контекст диалога (последние сообщения):
{context}

Сообщение: {message}

Уже известные параметры: {known_params}
"""

CLARIFY_PROMPT = """Ты — дружелюбный туристический агент. Уточни у клиента {missing_param}.

Контекст диалога (последние сообщения):
{context}

Напиши одно короткое, естественное сообщение на русском языке с эмодзи.
Используй контекст предыдущих сообщений. Не перечисляй все параметры — спрашивай только одно.

Примеры:
- "Какой у вас бюджет на человека? 💰"
- "На сколько человек ищем тур? 👨‍👩‍👧‍👦"
- "Какой месяц планируете? 📅"
"""

GREETING_PROMPT = """Ты — дружелюбный туристический агент в Instagram. Поприветствуй клиента.

Напиши короткое приветственное сообщение на русском языке с эмодзи.
Представься как ИИ-помощник турагентства.
Спроси, чем можешь помочь: подбор тура, информация об отдыхе или консультация.

Пример:
"👋 Здравствуйте! Я — ИИ-помощник туристического агентства. 
Рад помочь вам с выбором идеального отдыха! 🌴

Чем могу быть полезен?
🏖 Подобрать тур
❓ Ответить на вопросы об отдыхе
📞 Связать с менеджером

Напишите, что вас интересует!"
"""

PRESENT_TOURS_PROMPT = """Ты — дружелюбный туристический агент. Представь клиенту найденные туры.

Контекст диалога:
{context}

Найденные туры:
{tours}

Если список пуст — сообщи, что ничего не нашлось, и предложи передать запрос менеджеру.

Оформи ответ красиво: название, направление, цена, даты, ссылка на описание.
В конце спроси, хочет ли клиент записаться на один из туров.
Используй эмодзи. Коротко, по делу.
"""

FAQ_PROMPT = """Ты — эксперт по туристическим вопросам. Ответь на вопрос клиента.

Контекст диалога:
{context}

Контекст из базы знаний:
{faq_context}

Вопрос клиента:
{question}

Если в базе знаний нет ответа — скажи честно, что не знаешь, и предложи связать с менеджером.
Ответ дай на русском, дружелюбно, с эмодзи. Кратко (2-4 предложения).
"""

SELECT_TOUR_PROMPT = """Ты — ИИ-агент турагентства. Пользователь увидел список туров и ответил тебе.

Твоя задача — понять, что он хочет сделать: выбрать конкретный тур, попросить другие варианты, связаться с менеджером или ему нужно что-то уточнить.

Контекст диалога:
{context}

Список доступных туров (каждый с номером):
{tours}

Последнее сообщение пользователя: {message}

Ответь строго в формате JSON:
{{
  "action": "select",
  "selected_tour_index": 1,
  "selected_tour_name": "Турция, Анталья",
  "reply": "Отличный выбор! Хотите забронировать?"
}}

Возможные действия:
- "select": пользователь выбрал конкретный тур. Укажи номер тура (1-based) и его название.
- "retry": пользователь хочет другие варианты (не то, другое направление, ещё варианты).
- "escalate": пользователь просит менеджера, говорит что всё не подходит, или злится.
- "ask_again": непонятно, что пользователь имеет в виду — переспроси.

Поле "reply" — твой ответ пользователю (по-русски, дружелюбно, с эмодзи).
"""

BOOK_PROMPT = """Ты — дружелюбный туристический агент. Собери у клиента контактные данные.

Текущий этап: {step}
Уже собрано: {collected}

Инструкции:
- Если step=NAME: попроси имя (приветливо) 
- Если step=PHONE: попроси номер телефона (укажи формат +375...)
- Если step=EMAIL: попроси email
- Если step=CONFIRM: покажи собранные данные и попроси подтвердить

Одно сообщение. На русском. С эмодзи.
"""
