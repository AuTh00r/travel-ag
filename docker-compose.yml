services:
  travel-agent-bot:
    build: .
    container_name: travel-agent-bot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials.json:/app/credentials.json:ro
    restart: unless-stopped
