# Сессия: 25.06.2026 — Escalation redesign + туры + промпт

## Что сделано

### 1. Редизайн эскалации (по `docs/ESCALATION-REDESIGN.md`)

| Задача | Файл | Статус |
|--------|------|--------|
| Добавить `Контекст:` в маркер `===МЕНЕДЖЕР===` | `src/ai/prompts.py` | ✅ |
| `get_username()` + кеш 500 записей | `src/channels/instagram.py` | ✅ |
| `_extract_escalation()` → `(reason, context)` | `src/main.py` | ✅ |
| Новый формат уведомления: @handle, таймстемп, контекст, опциональные контакты | `src/services/telegram_notify.py` | ✅ |
| Переписаны тесты под новый формат | `tests/test_telegram_notify.py` | ✅ |
| Тесты `get_username` | `tests/test_instagram.py` | ✅ |
| Тесты парсинга контекста | `tests/test_engine.py` | ✅ |

### 2. Синхронизация туров

| Действие | Файлы |
|----------|-------|
| Удалены 8 устаревших туров | `tours/` (8 DOCX удалены) |
| Обновлены 5 туров с визовой информацией | `tours/` (5 DOCX изменены) |
| `.gitignore` для `_analysis.json` | `.gitignore` |

### 3. Промпт: визовые правила

- Добавлены правила обработки `Виза: НУЖНА` / `Виза: НЕ обязательно (открывается под тур)`
- При отсутствии визы у клиента — приоритет туров с пометкой «открывается под тур»

### 4. Промпт: лимиты

- Лимит символов поднят 900 → 980
- Добавлено `старайся использовать почти весь лимит`
- Правило ссылки усилено: «режь воду, но ссылку оставляй»

### 5. Баги по ходу

| Проблема | Решение |
|----------|---------|
| Эскалации не приходили в Telegram | LLM не генерировал маркер — `МОЖЕШЬ` заменён на `ОБЯЗАН`, `Контекст:` упрощён с 2-4 до 1-2 предложений |
| `get_username` не работал | Домен `graph.instagram.com` → `self.BASE_URL` (`graph.facebook.com`) |
| Бот рекомендовал удалённые туры | Не были закоммичены и запущены |

### 6. Лимит эскалаций

- В сессию добавлено `escalation_count`
- После 3 эскалаций от одного клиента — уведомления не отправляются
- LLM получает инструкцию `_ESCALATION_LIMIT_REACHED` не генерировать маркер

## Коммиты

```
cf8a883 feat: redesign escalation flow with context and instagram handle
443670f feat: add visa handling rules to LLM prompt
3644dfe chore: sync tours — delete 8 outdated, update 5 with visa info
32e5211 fix: raise char limit to 980, limit to 3 tours per message
f07132d fix: revert 3 -> 2-3 tours per message
ed6ca0c fix: enforce mandatory tour link, cut filler over link
7bf9c88 fix: remove generic Schengen help mention from visa rule
e977573 fix: make escalation mandatory, simplify Контекст to 1-2 sentences
2374847 fix: emphasize both contacts must be shown
4a7c983 fix: use graph.facebook.com for get_username API
c46ef8a feat: limit escalation to 3 per session per client
```

## Команды

```bash
# Деплой
git add <files> && git commit -m "msg"
git push origin master
ssh -i ~/.ssh/id_ed25519_travelbot root@201.51.3.72
cd /opt/travel-agent-bot
git pull origin master
systemctl restart travel-bot

# Сброс сессий
rm -f /opt/travel-agent-bot/data/sessions.db
systemctl restart travel-bot

# Логи
journalctl -u travel-bot -n 50 --no-pager
journalctl -u travel-bot -f

# Тесты
pytest tests/ -q
```
