# Сессия 2026-06-26 — Non-text обработка Instagram

## Контекст

Чат-бот турагентства «Сандита» (Минск). Стек: Python + FastAPI + DeepSeek API, Instagram Direct, ChromaDB RAG, SQLite-сессии, Google Sheets, Telegram-уведомления.

---

## 1. План

Файл: `docs/WORK-PLAN-instagram-non-text.md`

**Проблема:** `InstagramChannel.receive_message()` обрабатывал только `message.text`. Сообщения с вложениями/постами/ответами на истории без текста доходили до webhook, но не превращались в событие и терялись.

**Цель:** non-text сообщения создают `kind="user_non_text"`, передаются менеджеру через Telegram без вызова DeepSeek, клиент получает автоответ.

---

## 2. Реализация

### 2.1 `src/channels/instagram.py`

- **`_extract_non_text_metadata(message)`** — статический метод, детектирует `attachments`, `reply_to`, `referral`. Возвращает `{types, summary, has_text, text, raw_keys}` или пустой dict.
- **`receive_message()`** — теперь возвращает три типа событий: `kind="user"`, `kind="user_non_text"`, `kind="manager"`.
- **`_merge_sender_events()`** — мержит `user` и `user_non_text` события от одного sender в одном webhook POST. Meta присылает shared post и текст как два отдельных `messaging` entry.

### 2.2 `src/main.py`

- **`_process_non_text_safely()`** — обработчик без LLM:
  - Manager takeover gate
  - Telegram-уведомление менеджеру с типом и текстом клиента
  - Лимит 3 эскалации
  - Сохранение истории
  - Задержка 5s + проверка `_in_ai_processing` перед auto-ack (если AI уже обрабатывает текст — auto-ack не отправляется)
- **Webhook-ветка:**
  - `user_non_text` с текстом → AI (с аннотацией `[Клиент также отправил: ...]`)
  - `user_non_text` без текста → `_process_non_text_safely`
  - `_in_ai_processing` заполняется до создания задач (cross-request dedup)

### 2.3 `src/ai/prompts.py`

- Бот говорит о себе во множественном числе («мы», «нас», «нам»)
- Если прямо спросили «кто ты?» — «я ассистент менеджера»
- Смягчён тон для вложений: «Спасибо, мы не видим содержимое вложения, передали вопрос менеджеру»

### 2.4 Auto-ack текст

```
Спасибо! Мы не видим содержимое вложения, поэтому передали ваш вопрос менеджеру.
Он посмотрит и поможет 🙌
```

### 2.5 Telegram-уведомление

```
Клиент отправил не текстовое сообщение в Instagram.
Тип: вложение: image
Текст клиента: <текст или "без текста">
Бот не видит содержимое вложения/поста/истории.
```

---

## 3. Тесты

- **Parser (7):** attachments без/с текстом, reply_to, referral, plain text, bot echo, manager echo
- **Merge (3):** мерж одного sender, раздельные sender'ы, без non-text
- **Webhook (3):** attachment вызывает non-text handler, dedup, no-mid skip
- **Processing (4):** ack+notify, limit 3 skip, AI-pending skip, manager takeover silent

Итого: **136 тестов** (было 117).

---

## 4. Проблемы и фиксы

| Проблема | Фикс |
|---|---|
| Non-text события терялись | `_extract_non_text_metadata` + `kind="user_non_text"` |
| Два ответа бота на shared post с текстом | `_merge_sender_events()` (один POST) + `_in_ai_processing` + 5s delay (разные POST) |
| Meta присылает post и текст в разных POST | In-memory dict `_in_ai_processing`, TTL 30s |
| Бот отвечал «он», «она», «они» | Промпт: множественное число («мы»), исключение — «кто ты?» → «я ассистент» |
| Грубый тон автоответа | Смягчён: «Спасибо! Мы не видим содержимое...» |

---

## 5. Деплой

- **SSH:** `ssh -i ~/.ssh/id_ed25519_travelbot root@201.51.3.72`
- **Путь:** `/opt/travel-agent-bot`
- **Команды:** `git pull origin master`, `source .venv/bin/activate`, `pip install -r requirements.txt`, `pytest tests/ -q`, `systemctl restart travel-bot`
- **Health:** `https://travelagenttest.duckdns.org/health`
- **Логи:** `journalctl -u travel-bot -n 100 --no-pager | grep instagram.non_text`

---

## 6. Изменённые файлы

- `src/channels/instagram.py` — `_extract_non_text_metadata`, `_merge_sender_events`, обновлён `receive_message`
- `src/main.py` — `_process_non_text_safely`, webhook ветка, `_in_ai_processing`
- `src/ai/prompts.py` — множественное число, смягчённый тон
- `tests/test_instagram.py` — 17 новых тестов
- `docs/ARCHITECTURE.md` — обновлён поток
- `.agents/skills/travel-bot-dev/SKILL.md` — обновлён поток
- `docs/DEVELOPMENT.md` — новый файл с инструкциями
- `docs/WORK-PLAN-instagram-non-text.md` — план работ

---

## 7. Сессия 2 — Google Sheets, Telegram, форматирование

### Проблемы

| Проблема | Фикс |
|---|---|
| В Google Sheets `Направление`, `Бюджет`, `Количество` всегда пустые | Добавлены поля в промпт (`_ACTION_INSTRUCTIONS`) и парсинг в `_extract_booking()` |
| Уведомления в Telegram получал только один менеджер | Добавлен `TELEGRAM_SECONDARY_CHAT_ID` в config + `.env`; `TelegramNotifier` отправляет на оба ID |
| В Instagram диалоге видны `**текст**` (Markdown) | Промпт: запрет форматирования; бэкенд: `_TEXT_FORMATTING_RE` вырезает `*` и `**` из ответа |
| В сырых сессиях оставались старые данные с неправильными полями | Очищена `data/sessions.db` на VPS |

### Ключевые решения

- `.env` на VPS обновляется вручную через SSH (не в git)
- Ссылка на Google Sheets в Telegram — URL на отдельной строке вместо Markdown-ссылки, чтобы `__` в Sheet ID не ломали Markdown
- Escalation per client: лимит 3 на sender_id, хранится в сессии

### Изменённые файлы

- `src/ai/prompts.py` — `Направление`, `Бюджет`, `Количество` в блок брони; запрет форматирования
- `src/main.py` — `_extract_booking()` парсит новые поля; `_strip_markers()` вырезает Markdown
- `src/config.py` — `telegram_secondary_chat_id`
- `src/services/telegram_notify.py` — отправка на несколько chat_id
- `.env` (VPS) — `TELEGRAM_SECONDARY_CHAT_ID=1218542962`, добавлен вручную через ssh
- `tests/test_engine.py` — 3 новых теста для `_extract_booking`
- `tests/test_telegram_notify.py` — патч secondary_chat_id

---

## 8. Сессия 3 — Telegram Markdown → plain text

### Проблема

Telegram-уведомления об эскалации падали с ошибкой `400 Bad Request: can't parse entities: Can't find end of the entity starting at byte offset 350` на втором аккаунте (1218542962), хотя первый (904138085) получал то же сообщение без ошибок. Root cause: `__` (double underscore) в Google Sheet ID `1YIU2UL__ZiekGnJcfgXPT8sGTYTwof6jPJyxxdRVDHg` интерпретировался Markdown-парсером Telegram как форматирование.

### Предыдущие фиксы (не помогли)

1. `_escape_md()` — экранировала `_`, `*`, `` ` `` для `context` (но не для `tag`, `handle`, `client_name`, `client_phone`, `client_email`)
2. Ссылка на Sheets вынесена на отдельную строку, убран Markdown-синтаксис `[text](url)` — не помогло, т.к. ошибка была в другом месте

### Решение

Убран `parse_mode: "Markdown"` полностью — уведомления отправляются как plain text. Emoji и URL работают, форматирование (bold/italic) потеряно, но доставка гарантирована. `_escape_md()` удалена как бесполезная.

### Ключевые решения

- `_send()` в `notify_manager` использует for-loop с try/except — если один chat_id падает, остальные выполняются, но `last_exc` пробрасывается после цикла
- Instagram DM (`src/channels/instagram.py:253`) всегда отправлял plain text — форматирование не поддерживается

### Изменённые файлы

- `src/services/telegram_notify.py` — удалён `parse_mode`, удалена разметка `*текст*` и `` `текст` `` из шаблонов, удалена `_escape_md()`
- `tests/test_telegram_notify.py` — убран assert на `parse_mode`
